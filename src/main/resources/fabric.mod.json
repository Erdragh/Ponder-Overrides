{
  "schemaVersion": 1,
  "id": "${archives_base_name}",
  "version": "${mod_version}",
  "name": "Ponder-Overrides",
  "description": "Allows loading different Ponders via KubeJS event",
  "authors": [
    {
      "name": "Erdragh",
      "contact": {
        "email": "mail@erdragh.dev"
      }
    }
  ],
  "contact": {
    "sources": "https://github.com/Erdragh/Ponder-Overrides"
  },
  "license": "MIT",
  "icon": "assets/ponder_overrides/icon.png",
  "environment": "*",
  "entrypoints": {
    "client": [
      "dev.erdragh.ponder_overrides.PonderOverrides"
    ]
  },
  "mixins": [
    "ponder_overrides.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "create": ">=${create_version}",
    "minecraft": "${minecraft_version}",
    "kubejs": "${kubejs_version}"
  }
}
